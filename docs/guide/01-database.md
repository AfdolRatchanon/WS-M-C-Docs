# Step 1 — ตั้งค่าฐานข้อมูล MySQL

> 🎯 **Analogy:** ฐานข้อมูลคือ "ตู้เก็บเอกสาร" ของ API — ก่อนจะเริ่มเขียนโปรแกรม ต้องเตรียมตู้ให้พร้อมก่อน

## ฐานข้อมูลคืออะไร และทำไม API ถึงต้องการ?

**API** คือตัวกลางระหว่าง Client กับข้อมูล เมื่อ Client ขอดูรายการเพลง API ต้องไปดึงข้อมูลมาจากที่ไหนสักที่ — นั่นคือ **ฐานข้อมูล**

ในโปรเจ็คนี้ใช้ **MySQL** ซึ่งเป็น Relational Database ที่เก็บข้อมูลเป็นตาราง (Table) คล้ายกับ Excel sheet ที่ตารางต่างๆ เชื่อมกันด้วย Foreign Key

```
Client ──── API (Node.js) ──── MySQL Database
              ↕ query               ↕ data
           routes/*.js         ตาราง users, albums, songs...
```

## โครงสร้างฐานข้อมูล `04_module_c`

ฐานข้อมูลนี้มี 6 ตาราง โดยแต่ละตารางมีหน้าที่ดังนี้:

| ตาราง | หน้าที่ | คอลัมน์สำคัญ |
|:------|:-------|:------------|
| `users` | เก็บข้อมูลผู้ใช้ | `username`, `password_hash`, `role`, `token`, `is_banned` |
| `albums` | อัลบั้มเพลง | `title`, `artist`, `genre`, `publisher_id` |
| `songs` | เพลงแต่ละเพลง | `title`, `lyrics`, `track_order`, `deleted_at` |
| `labels` | ป้ายกำกับ เช่น Pop, Rock | `name` |
| `song_labels` | เชื่อม song กับ label | `song_id`, `label_id` |
| `user_view_logs` | บันทึกการดูเพลง | `user_id`, `song_id` |

::: tip 💡 ทำไมต้องแยกหลายตาราง?
เพราะข้อมูลมีความสัมพันธ์กัน เช่น เพลง 1 เพลงมีได้หลาย Label และ 1 Label มีได้หลายเพลง — ถ้าเก็บรวมในตารางเดียวจะซ้ำซ้อนมาก การแยกตารางและใช้ Foreign Key เรียกว่า **Database Normalization**
:::

## ขั้นตอนการตั้งค่า

### 1. เปิด XAMPP

1. เปิดโปรแกรม **XAMPP Control Panel**
2. กด **Start** ที่ **Apache** และ **MySQL**
3. รอจนไฟสีเขียวทั้งสอง

::: warning ⚠️ ถ้า MySQL Start ไม่ได้
อาจมีโปรแกรมอื่นใช้ Port 3306 อยู่ ลองปิด SQL Server หรือโปรแกรม Database อื่นก่อน
:::

### 2. เปิด phpMyAdmin

เปิด Browser ไปที่ `http://localhost/phpmyadmin`

### 3. สร้าง Database

1. คลิกแท็บ **Databases** ด้านบน
2. พิมพ์ชื่อ `04_module_c`
3. Collation: เลือก `utf8mb4_general_ci`
4. กด **Create**

### 4. Import ข้อมูลจากไฟล์ SQL

1. คลิกเลือก `04_module_c` ในแถบซ้าย
2. คลิกแท็บ **Import** ด้านบน
3. กด **Choose File** → เลือกไฟล์ `module_c_db.sql` จากโฟลเดอร์โปรเจ็ค
4. กด **Import** (ปุ่มสีน้ำเงินด้านล่าง)

::: details ✅ ผลลัพธ์ที่ถูกต้อง
ควรเห็นข้อความ "Import has been successfully finished" และแถบซ้ายแสดง 6 ตาราง:
- `albums`
- `labels`
- `song_labels`
- `songs`
- `user_view_logs`
- `users`
:::

### 5. ตรวจสอบข้อมูลตัวอย่าง

คลิกตาราง `users` → แท็บ **Browse** — ควรเห็น 4 user:

| user_id | username | role | is_banned |
|:-------:|:---------|:----:|:---------:|
| 1 | admin | admin | 0 |
| 2 | user1 | user | 0 |
| 3 | user2 | user | 0 |
| 4 | user3 | user | 1 |

::: tip 💡 Password ใดที่ใช้ทดสอบ?
- `admin` → password: `adminpass`
- `user1` → password: `user1pass`
- `user2` → password: `user2pass`
- `user3` → password: `user3pass` (ถูกแบนแล้ว — ใช้ทดสอบ 403 Forbidden ได้)

Password ถูก hash ด้วย bcrypt ไว้แล้ว API จะ compare ให้อัตโนมัติ
:::

## Glossary

| คำศัพท์ | ความหมาย |
|:--------|:---------|
| **XAMPP** | โปรแกรม bundle Apache + MySQL + PHP ใช้สร้าง local server สำหรับ dev |
| **MySQL** | Relational Database Management System ที่เก็บข้อมูลเป็นตาราง |
| **phpMyAdmin** | Web UI สำหรับจัดการ MySQL — สร้าง DB, import SQL, ดูข้อมูล ผ่าน browser |
| **Database** | ที่เก็บข้อมูลแบบมีโครงสร้าง |
| **Table** | ตารางข้อมูล คล้าย Excel sheet |
| **Column** | คอลัมน์ในตาราง (เช่น `username`) |
| **Row** | แถวข้อมูล 1 รายการ |
| **Primary Key** | คอลัมน์ที่ระบุแถวได้ไม่ซ้ำกัน (เช่น `user_id`) |
| **AUTO_INCREMENT** | สร้างเลข Primary Key ให้อัตโนมัติ เพิ่มทีละ 1 ทุกครั้งที่ insert |
| **Foreign Key** | คอลัมน์ที่อ้างอิง Primary Key ของตารางอื่น |
| **UNIQUE** | Constraint ป้องกันค่าซ้ำในคอลัมน์ — เช่น `username`, `email` ต้องไม่ซ้ำ ถ้าซ้ำ → 409 |
| **Collation** | การเรียงลำดับตัวอักษร `utf8mb4` รองรับภาษาไทยและ Emoji |
| **SQL** | ภาษาสำหรับสั่งงาน Relational Database เช่น SELECT, INSERT, UPDATE, DELETE |
| **Import** | นำข้อมูลจากไฟล์ `.sql` เข้าสู่ฐานข้อมูล |
| **Normalization** | การออกแบบ DB แยกข้อมูลเป็นหลายตารางเพื่อลดความซ้ำซ้อน |
| **Many-to-Many** | ความสัมพันธ์ที่ 1 รายการมีได้หลายอีกรายการ — ใช้ตารางกลาง เช่น `song_labels` |
| **bcrypt** | Algorithm เข้ารหัส password แบบ one-way — ถอดรหัสกลับไม่ได้ ต้อง compare แทน |
| **Soft Delete** | ลบแบบ "ซ่อน" โดยใส่เวลาใน `deleted_at` แทนการลบจริง — กู้คืนได้ |

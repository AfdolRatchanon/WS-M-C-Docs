# Step 2 — สร้างโปรเจ็ค Node.js + Express

> 🎯 **Analogy:** Node.js คือ "เครื่องยนต์" และ Express คือ "โครงรถยนต์" — เครื่องยนต์อย่างเดียวขับไปไหนไม่ได้ ต้องมีโครงรถให้ขับ

## Node.js คืออะไร?

ปกติ JavaScript ทำงานได้แค่ใน Browser เท่านั้น **Node.js** คือ Runtime ที่ทำให้ JavaScript รันบน Server ได้ด้วย — เราจึงเขียน Backend ด้วย JavaScript ภาษาเดียวกับ Frontend ได้เลย

## Express คืออะไร?

Node.js มี built-in `http` module สำหรับสร้าง Server แต่ต้องเขียนโค้ดเยอะมาก **Express** คือ framework ที่ wrap สิ่งเหล่านั้นไว้ ทำให้สร้าง Route, Middleware, และ Server ได้ง่ายขึ้นมาก

```js
// สร้าง server ด้วย http module (ยาว)
const http = require('http')
http.createServer((req, res) => {
  if (req.url === '/api' && req.method === 'GET') {
    res.writeHead(200, { 'Content-Type': 'application/json' })
    res.end(JSON.stringify({ message: 'Hello' }))
  }
}).listen(3000)

// สร้าง server ด้วย Express (สั้นกว่ามาก)
const express = require('express')
const app = express()
app.get('/api', (req, res) => res.json({ message: 'Hello' }))
app.listen(3000)
```

## npm คืออะไร?

**npm (Node Package Manager)** คือตัวจัดการ Package ของ Node.js ใช้ติดตั้ง library ต่างๆ เช่น Express, multer, bcryptjs

```
npm install <package>    → ดาวน์โหลดและติดตั้ง package
npm init                 → สร้างไฟล์ package.json
node_modules/            → โฟลเดอร์ที่เก็บ package ที่ติดตั้งแล้ว
package.json             → ไฟล์บอก project ใช้ package อะไรบ้าง
```

## ขั้นตอน

### 2.1 สร้าง package.json

เปิด Terminal ในโฟลเดอร์โปรเจ็ค แล้วรัน:

```bash
npm init -y
```

Flag `-y` ตอบ Yes ทุกคำถามโดยอัตโนมัติ — ได้ `package.json` ที่มีค่า default ทันที

::: details ✅ package.json ที่ได้
```json
{
  "name": "demo-module-c",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
```
:::

### 2.2 ติดตั้ง Express

```bash
npm install express
```

หลังรันคำสั่งนี้:
- โฟลเดอร์ `node_modules/` จะถูกสร้างขึ้น
- `package.json` จะมี `"dependencies": { "express": "^4.x.x" }` เพิ่มขึ้นมา

### 2.3 สร้างไฟล์ `app.js`

สร้างไฟล์ `app.js` ที่ root ของโปรเจ็ค:

```js
const express = require('express')  // โหลด Express มาใช้
const app = express()               // สร้าง Express application

// Route แรก: ตอบกลับเมื่อมี GET request ที่ /
app.get('/', (req, res) => {
  res.json({ message: 'Hello World!' })
})

// เริ่ม Server ที่ Port 3000
app.listen(3000, () => {
  console.log('Server running on http://localhost:3000')
})
```

**คำอธิบายทีละบรรทัด:**
- `require('express')` — โหลด library Express มาใช้งาน
- `express()` — สร้าง Express app instance
- `app.get('/', ...)` — ลงทะเบียน Route: เมื่อมี GET request มาที่ `/` ให้ทำฟังก์ชันนี้
- `req` (Request) — ข้อมูลที่ client ส่งมา
- `res` (Response) — ใช้ส่งข้อมูลกลับ client
- `res.json(...)` — ส่งข้อมูลกลับเป็น JSON format
- `app.listen(3000, ...)` — เริ่ม Server รับ request ที่ Port 3000

### 2.4 ทดสอบ

```bash
node app.js
```

เปิด Browser หรือ Postman ไปที่ `http://localhost:3000`

::: details ✅ ผลลัพธ์ที่ถูกต้อง
```json
{ "message": "Hello World!" }
```
:::

กด `Ctrl+C` เพื่อหยุด server

## HTTP Methods และ Status Codes

ก่อนไปต่อ ควรรู้จัก HTTP Methods — วิธีที่ Client บอก Server ว่าต้องการทำอะไร:

| Method | ความหมาย | ตัวอย่าง |
|:-------|:---------|:--------|
| `GET` | ขอดูข้อมูล | ดูรายการเพลง |
| `POST` | สร้างข้อมูลใหม่ | เพิ่มเพลงใหม่ |
| `PUT` | แก้ไขข้อมูล | อัปเดตชื่อเพลง |
| `DELETE` | ลบข้อมูล | ลบเพลง |

และ HTTP Status Codes — รหัสที่ Server ตอบบอกผลลัพธ์:

| Code | ความหมาย | ใช้เมื่อ |
|:-----|:---------|:--------|
| `200` | OK | สำเร็จ (GET, PUT) |
| `201` | Created | สร้างข้อมูลใหม่สำเร็จ |
| `400` | Bad Request | ส่งข้อมูลไม่ครบ |
| `401` | Unauthorized | ไม่ได้ login / token ผิด |
| `403` | Forbidden | login แล้วแต่ไม่มีสิทธิ์ |
| `404` | Not Found | ไม่เจอข้อมูล |
| `409` | Conflict | ข้อมูลซ้ำ (เช่น username ซ้ำ) |
| `500` | Internal Server Error | Server มีปัญหา |

## Glossary

| คำศัพท์ | ความหมาย |
|:--------|:---------|
| **Runtime** | สภาพแวดล้อมสำหรับรันโปรแกรม |
| **Framework** | โครงสร้างพื้นฐานที่ช่วยให้เขียนโค้ดได้ง่ายขึ้น |
| **Package** | Library หรือเครื่องมือที่คนอื่นสร้างไว้แล้ว เราดาวน์โหลดมาใช้ |
| **Route** | การกำหนดว่า URL + Method ไหน ให้ทำอะไร |
| **Request (req)** | ข้อมูลที่ Client ส่งมาให้ Server |
| **Response (res)** | ข้อมูลที่ Server ส่งกลับไปให้ Client |
| **Port** | ช่องทางการสื่อสารใน Network (ใช้ 3000 สำหรับ dev) |
| **JSON** | รูปแบบข้อมูล key-value ที่ใช้แลกเปลี่ยนระหว่าง API |

# Step 3 — nodemon (Auto-restart)

> 🎯 **Analogy:** nodemon คือ "นาฬิกาปลุก" ที่คอยตรวจดูว่าไฟล์เปลี่ยนไหม — ถ้าเปลี่ยน ปลุก server ให้ restart ทันที

## ปัญหาที่ nodemon แก้

ตอนนี้ทุกครั้งที่แก้โค้ดใน `app.js` ต้องทำแบบนี้:

```
1. กด Ctrl+C เพื่อหยุด server
2. รัน node app.js ใหม่
3. ทดสอบ
4. แก้โค้ดอีก → วนซ้ำข้อ 1
```

น่าเบื่อมาก และเสียเวลา — **nodemon** แก้ปัญหานี้โดยคอย Watch ไฟล์ เมื่อตรวจพบการเปลี่ยนแปลง จะ restart server ให้อัตโนมัติ

## ติดตั้ง nodemon

```bash
npm install -D nodemon
```

Flag `-D` (หรือ `--save-dev`) หมายถึง **devDependency** — ติดตั้งเฉพาะตอน Development ไม่ต้องมีใน Production

::: tip 💡 devDependency vs dependency
- `dependencies` — ต้องใช้ทั้ง dev และ production (เช่น express, mysql2)
- `devDependencies` — ใช้แค่ตอน dev เท่านั้น (เช่น nodemon)

เวลา Deploy ขึ้น server จริง สามารถ `npm install --production` เพื่อข้ามการติดตั้ง devDependencies ได้
:::

## เพิ่ม Scripts ใน package.json

เปิดไฟล์ `package.json` แล้วแก้ส่วน `scripts`:

```json
"scripts": {
  "start": "node app.js",
  "dev": "nodemon app.js"
}
```

**คำอธิบาย:**
- `start` — ใช้ตอน Production: รัน server ปกติ ไม่ watch ไฟล์
- `dev` — ใช้ตอน Development: รันด้วย nodemon — restart อัตโนมัติ

::: warning ⚠️ ข้อผิดพลาดที่พบบ่อย
`"dev": "nodemon node app.js"` — **ผิด!** `nodemon` และ `node` เป็นคำสั่งคนละอัน อย่าใส่ทั้งสอง

คำสั่งที่ถูกคือ `"dev": "nodemon app.js"` — nodemon จะใช้ node รันไฟล์ให้เอง
:::

## ทดสอบ nodemon

```bash
npm run dev
```

Terminal จะแสดง:
```
[nodemon] 3.x.x
[nodemon] watching path(s): *.*
[nodemon] watching extensions: js,mjs,cjs,json
[nodemon] starting `node app.js`
Server running on http://localhost:3000
```

ลองแก้ข้อความใน `app.js` เป็น `"Hello Express!"` แล้ว Save — nodemon จะ restart ให้อัตโนมัติ ไม่ต้องกด Ctrl+C เอง

::: details ✅ ผลลัพธ์หลัง Save
```
[nodemon] restarting due to changes...
[nodemon] starting `node app.js`
Server running on http://localhost:3000
```
:::

::: tip 💡 จากนี้ไปใช้ `npm run dev` เสมอ
ใช้คำสั่งนี้แทน `node app.js` ตลอดการพัฒนา กด `Ctrl+C` เมื่อต้องการหยุด
:::

## Glossary

| คำศัพท์ | ความหมาย |
|:--------|:---------|
| **nodemon** | เครื่องมือ Watch ไฟล์และ restart Node.js อัตโนมัติ |
| **devDependency** | Package ที่ใช้เฉพาะตอนพัฒนา ไม่ใช่ Production |
| **npm run** | รัน script ที่กำหนดใน `package.json` |
| **Watch** | การตรวจสอบการเปลี่ยนแปลงของไฟล์อย่างต่อเนื่อง |
| **Hot Reload** | การ reload application อัตโนมัติเมื่อโค้ดเปลี่ยน |
